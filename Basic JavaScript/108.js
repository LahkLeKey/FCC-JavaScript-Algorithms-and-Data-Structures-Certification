/*
    FCC Version - 7.0
    Use the parseInt Function
    Task - 108 / 111
    Goal : Use parseInt() in the convertToInteger function so it converts the input string str into an integer, and returns it.
*/

function convertToInteger(str) {
  return parseInt(str);
}

convertToInteger("56");
